{
  "name": "@moto-tracker/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for Moto Tracker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.1"
  }
}
